export function renderCollaboration(p,{esc}) {
 const c=p.collaboration;
 return `<section class="project-collaboration" aria-labelledby="${p.slug}-collaboration-title"><h4 id="${p.slug}-collaboration-title">${esc(c.title)}</h4><ul>${c.prompts.map(t=>`<li>${esc(t)}</li>`).join('')}</ul><p>${esc(c.invitation)}</p><form hidden data-collaboration-form data-recipient="${esc(c.email)}" data-subject-prefix="${esc(c.subjectPrefix)}"><label for="${p.slug}-email">Your email</label><input id="${p.slug}-email" name="email" type="email" autocomplete="email" maxlength="254" required><label for="${p.slug}-subject">Subject</label><div class="collaboration-subject"><span>${esc(c.subjectPrefix)}</span><input id="${p.slug}-subject" name="subject" type="text" maxlength="120" placeholder="Your collaboration idea" required></div><label for="${p.slug}-message">Short message</label><textarea id="${p.slug}-message" name="message" rows="5" maxlength="2000" required></textarea><button type="submit">Open email draft and send</button><p role="status" data-collaboration-status></p></form><noscript><p>Email <a href="mailto:${esc(c.email)}?subject=SEER%20Collaboration%20-%20">${esc(c.email)}</a> to collaborate.</p></noscript></section>`;
}
